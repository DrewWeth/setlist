<%= form_with(model: set_list, local: true) do |form| %>
  <% if set_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(set_list.errors.count, "error") %> prohibited this set_list from being saved:</h2>

      <ul>
        <% set_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :gig_date %>
    <%= form.datetime_select :gig_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
